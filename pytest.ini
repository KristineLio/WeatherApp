[pytest]
testpaths = tests

python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: tests that check multiple app components working together
    smoke: basic import/startup checks
    unit: fast isolated tests for one module/class/function

addopts =
    -ra
    --strict-markers